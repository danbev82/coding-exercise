
1. clone repo from git ---
2. open twigTask.js and amend values:
    a. uncomment out section to adjust values
    b. comment out test cases if required
3. open command line / git bash and cd to twig\src and execute file using node
  -- node twigTask.js
4. You will see result printed in console. 